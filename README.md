# Project Setup
Clone the repository and go into the project folder. In one terminal run:

cd backend
npm install

create a .env file inside backend with:

PORT=4000
MONGO_URI=mongo db atlas url

then run:

npm run dev

In a second terminal run:

cd frontend
npm install
npm run dev

Frontend runs on http://localhost:5173 and backend runs on http://localhost:4000
